from django.shortcuts import render

def saving(request):
    context={

    }
    return render(request, "deposit/app/saving.html", context)

def int_rate(request):
    context={

    }
    return render(request, "deposit/app/int_rate.html", context)
